import SwiftUI

enum Theme {
    static let brand = Color(red: 0x0C / 255.0, green: 0x3B / 255.0, blue: 0x1C / 255.0)
    static let brandSoft = Color(red: 0x1A / 255.0, green: 0x55 / 255.0, blue: 0x2C / 255.0)
    static let surface = Color.black.opacity(0.55)
}
